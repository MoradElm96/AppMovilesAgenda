<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="morad.myapp">
<!-- perimisos para camara,flas,lectura y escritura en storage-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_index"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_index_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.MyApp">

        <activity  android:name="com.morad.myapp.Resultado"    android:exported="false" />
        <activity   android:name="com.morad.myapp.MainActivity"  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- //para el tema -->
        <activity   android:name="com.morad.myapp.InformacionClase" android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity android:name="com.morad.myapp.Crud" ></activity>
        <activity android:name="com.morad.myapp.Linterna"></activity>
        <activity android:name="com.morad.myapp.Calculadora"></activity>
        <activity  android:name="com.morad.myapp.VerPersonas"></activity>
    </application>

</manifest>